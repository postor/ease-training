# database
MYSQL_DATABASE=ease_training
MYSQL_ROOT_PASSWORD=ease_training
MYSQL_HOST=mysql

# restful
RESTFUL_ENDPOINT=http://webapp:3000/restful

# runner image
RUNNER_IMAGE=postor/ease-training

# run docker with --shm-size
SHM_SIZE=32G

# run train_xxx.py with --batch-size and --gpus
DEFAULT_BATCH_SIZE=1
DEFAULT_GPUS=0

# shared-files for runner path is auto generated (with pwd in ./prepare.sh)